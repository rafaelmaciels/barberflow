@extends('layouts.app') @section('title', 'Barbeiros') @section('content')
<div class="row mb-4"> <div class="col-12 d-flex justify-content-between align-items-center"> <h2 class="fw-bold text-primary mb-0"><i class="fa-solid fa-scissors me-2"></i> Barbeiros</h2> <a href="{{ route('barbers.create') }}" class="btn btn-primary fw-bold px-4 rounded-pill shadow-sm"> <i class="fa-solid fa-plus me-1"></i> Novo Barbeiro </a> </div>
</div> <div class="card shadow-sm border-0 rounded-4"> <div class="card-body p-4"> @if(session('success')) <div class="alert alert-success alert-dismissible fade show" role="alert"> {{ session('success') }} <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> </div> @endif <div class="table-responsive"> <table class="table table-hover align-middle" id="barbersTable"> <thead class="table-light"> <tr> <th>Foto</th> <th>Nome</th> <th>Contato</th> <th>Status</th> <th>Ativo</th> <th class="text-end">Ações</th> </tr> </thead> <tbody> @foreach($barbers as $barber) <tr> <td> @if($barber->foto) <img src="{{ asset($barber->foto) }}" alt="{{ $barber->nome }}" class="rounded-circle" width="50" height="50" style="object-fit: cover;"> @else <div class="bg-secondary text-white rounded-circle d-flex justify-content-center align-items-center fw-bold" style="width: 50px; height: 50px;"> {{ substr($barber->nome, 0, 1) }} </div> @endif </td> <td class="fw-semibold">{{ $barber->nome }}</td> <td> <div><i class="fa-solid fa-envelope text-muted small me-1"></i> {{ $barber->email }}</div> @if($barber->telefone) <div><i class="fa-solid fa-phone text-muted small me-1"></i> {{ $barber->telefone }}</div> @endif </td> <td> @if($barber->status == 'livre') <span class="badge bg-success">Livre</span> @elseif($barber->status == 'ocupado') <span class="badge bg-danger">Ocupado</span> @else <span class="badge bg-warning text-body">Ausente</span> @endif </td> <td> @if($barber->ativo) <span class="badge bg-primary">Sim</span> @else <span class="badge bg-secondary">Não</span> @endif </td> <td class="text-end"> <a href="{{ route('barbers.edit', $barber->id) }}" class="btn btn-sm btn-outline-primary rounded-3 me-2"> <i class="fa-solid fa-pen-to-square"></i> Editar </a> <form action="{{ route('barbers.destroy', $barber->id) }}" method="POST" class="d-inline form-delete"> @csrf @method('DELETE') <button type="button" class="btn btn-sm btn-outline-danger rounded-3 btn-delete"> <i class="fa-solid fa-trash"></i> </button> </form> </td> </tr> @endforeach </tbody> </table> </div> </div>
</div> <script> document.addEventListener('DOMContentLoaded', function() { // Inicializa o DataTables, se já estiver injetado no app.js if (typeof jQuery !== 'undefined' && $.fn.DataTable) { $('#barbersTable').DataTable({ language: { url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/pt-BR.json' }, ordering: true, info: false }); } // SweetAlert2 para exclusão document.querySelectorAll('.btn-delete').forEach(button => { button.addEventListener('click', function() { const form = this.closest('form'); if (typeof Swal !== 'undefined') { Swal.fire({ title: 'Tem certeza?', text:"Você não poderá reverter a exclusão deste barbeiro!", icon: 'warning', showCancelButton: true, confirmButtonColor: '#d33', cancelButtonColor: '#6c757d', confirmButtonText: 'Sim, excluir!', cancelButtonText: 'Cancelar' }).then((result) => { if (result.isConfirmed) { form.submit(); } }); } else { if (confirm('Tem certeza que deseja excluir?')) { form.submit(); } } }); }); });
</script>
@endsection

